import React from "react";

const WelcomePage = () => {
  return <div style={{padding: "15% 0", textAlign: 'center'}}>
    <h1>Welcome Gklsan Employment management application with React-router and ReactJS hooks(createContext, useContext, useState)</h1>
    <h3>- Gokul</h3>
  </div>
};

export default WelcomePage;
